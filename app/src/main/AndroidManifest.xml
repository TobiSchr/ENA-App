<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lukasgiesler.kassenabschluss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".KassenuebersichtActivity"
            android:label="Kassen Übersicht"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".KassenabschlusslisteActivity"
            android:label="Kassenabschluss Liste"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".NeuerKassenabschlussActivity"
            android:label="Neuer Kassenabschluss"
            android:screenOrientation="portrait"
            android:noHistory="true">
        </activity>

        <activity
            android:name=".KassenabschlussDetailsActivity"
            android:label="Kassenabschluss Details"
            android:screenOrientation="portrait"
            android:noHistory="true">
        </activity>

    </application>

</manifest>